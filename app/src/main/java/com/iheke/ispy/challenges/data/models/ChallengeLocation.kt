package com.iheke.ispy.challenges.data.models

/**
 * ChallengeLocation is a data class representing the latitude and longitude coordinates
 * of a challenge location.
 *
 * @param latitude The latitude coordinate of the challenge location.
 * @param longitude The longitude coordinate of the challenge location.
 */
data class ChallengeLocation(
    val latitude: Double,
    val longitude: Double
)
